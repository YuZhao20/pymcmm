This file is for setting up a development or experiment environment.
To install the package in editable mode along with experiment dependencies:
pip install -r requirements.txt
Install the local package in editable mode

-e .
Dependencies for running benchmarks and visualization

kmodes
matplotlib
seaborn
